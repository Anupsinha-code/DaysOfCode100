#include <stdio.h>
int main()
{
    int first, second , sum;
    printf("Enter 1st number :");
    scanf("%d",&first);
    printf("Enter 2nd Number :");
    scanf("%d", &second);
    printf("Sum is%d",first+second );
    printf(",Diff%d", first-second );
    printf(",Product%d", first*second );
    printf(",quotient%d",first/second );
}